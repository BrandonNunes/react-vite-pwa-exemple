import styled from "styled-components";
import {PADDINGSIZE} from "@/GlobalStyles";

const Container = styled.main`
  width: 100%;
  height: calc(100vh - ${PADDINGSIZE * 2}px);
  background-color: #ffffff;
`

export default Container;

